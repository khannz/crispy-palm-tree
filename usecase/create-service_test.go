package usecase

import (
	"testing"
)

// TestValidateRemoveApplicationServers ...
func CreateService(t *testing.T) {
	// assert := assert.New(t)
	// locker := &domain.Locker{}
	// gracefulShutdown := &domain.GracefulShutdown{}
	// createService := NewCreateServiceEntity(locker)
}
